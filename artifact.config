[
  {artifact, [
      [{rpc, [
        port: 11011,
        process_limit, 120
      ]}]

      {buckets, 2048}, 
      {vnodes, 256},
      {tables, 512},
      {store, ets},

      [{interfaces, [
        {max_connections, 128}
        [{memcache, [
          {port, 11211},
          {process_limit, 10},
        ]}]
      ]}]

      {n, 1},
      {r, 1},
      {w, 1},
      {hash_function, md5}
  ]}

  %% % Logger related items 
  {lager, [
    {colored, true},
    {handlers, [
      {lager_console_backend, info},
      {lager_file_backend, [{file, "error.log"}, {level, error}]},
      {lager_file_backend, [{file, "console.log"}, {level, info}]}
    ]}
  ]}.

]
